import React from "react";
import { useLocation, Link } from "react-router-dom";

export function Collections(){

    return(
            <div className="flex min-h-full justify-center align-middle">
                this is Collectioomns page
            </div>
    )
}